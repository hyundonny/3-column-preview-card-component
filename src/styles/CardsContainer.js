import styled from "styled-components";

const CardContainer = styled.div`
  width: 100%;
  max-width: 320px;
  margin: 0 auto;
  overflow: hidden;
  border-radius: 1rem;
`;

export default CardContainer;
